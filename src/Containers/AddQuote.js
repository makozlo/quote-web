import * as React from 'react';
import { Formik } from 'formik';

const { Component } = React;

type Props = {};
type State = {};

class AddQuote extends Component<Props, State> {
  render() {
    return <div />;
  }
}